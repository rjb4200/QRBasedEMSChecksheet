## Context

The kits page header currently has a red label "Shared Layouts" and a black H1 "Kits". The Fleet Panel and Units pages use the large black title for the page name with red labels for sub-sections. Kits should follow the same pattern.

## Decisions

Swap "Shared Layouts" to H1 (`text-4xl font-black`) and remove "Kits". Style "Create Kit" and related subheadings with red text to match how "Fleet Matrix" and "User Management" appear as red labels on their respective pages.
