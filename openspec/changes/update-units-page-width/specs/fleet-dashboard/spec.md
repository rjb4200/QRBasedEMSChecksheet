## ADDED Requirements

### Requirement: Admin units page uses standard admin page width
The admin units page SHALL use `max-w-7xl` as its content section width, consistent with other admin pages.

#### Scenario: Units page matches standard width
- **WHEN** the admin units page renders
- **THEN** the content section SHALL use `max-w-7xl`
