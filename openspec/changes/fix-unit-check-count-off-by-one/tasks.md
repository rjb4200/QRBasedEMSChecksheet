## 1. Shift Reset Edge Function

- [x] 1.1 Update `supabase/functions/shift-reset/index.ts` to include crew-name lock target in archive `total_compartments` and `completed_compartments`.

## 2. Unit Page Display

- [x] 2.1 Update "Previous shift" section in `src/app/units/[id]/page.tsx` to add crew target to the displayed counts.

## 3. Verification

- [x] 3.1 Run `npm run typecheck` and `npm run lint`.
