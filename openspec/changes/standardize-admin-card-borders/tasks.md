## 1. Fix list item borders on Units page

- [x] 1.1 Change unit list item cards from `border border-slate-200` to `border-2 border-slate-200` in `src/app/admin/units/page.tsx`
- [x] 1.2 Add `border-2 border-slate-200` to the unit list container card in `src/app/admin/units/page.tsx`

## 2. Fix list item borders on Fleet Matrix

- [x] 2.1 Change Fleet Matrix unit cards from `border border-slate-200` to `border-2 border-slate-200` in `src/components/fleet-matrix.tsx`

## 3. Fix borders on Equipment catalog rows

- [x] 3.1 Change read-mode catalog rows from `border border-slate-200` to `border-2 border-slate-200` in `src/app/admin/equipment/editable-catalog-row.tsx`

## 4. Fix Issues list container

- [x] 4.1 Change Issues list container from `border border-slate-200` to `border-2 border-slate-200` in `src/app/admin/issues/page.tsx`
- [x] 4.2 Add `p-5` padding to the Issues list container to match other list containers

## 5. Verification

- [x] 5.1 Run `npm run build` to verify no compilation errors from class string changes
