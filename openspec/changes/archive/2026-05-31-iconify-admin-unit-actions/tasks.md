## 1. Icon Extraction

- [x] 1.1 Create `src/components/icons.tsx` with `IconEdit` and `IconTrash` SVGs extracted from `editable-catalog-row.tsx`.
- [x] 1.2 Update `editable-catalog-row.tsx` to import icons from the shared file instead of defining them locally.
- [x] 2.1 Replace "Edit" text button with `IconEdit` button (Link with pencil icon and accessible label).
- [x] 2.2 Replace "Delete" text button with `IconTrash` button (form with trash icon and accessible label).
- [x] 2.3 Move OOS toggle to the leftmost position in the button row.
- [x] 3.1 Run TypeScript typecheck.
- [x] 3.2 Manually verify icon buttons appear correctly and OOS toggle stays leftmost.
