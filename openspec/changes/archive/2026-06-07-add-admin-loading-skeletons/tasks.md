## 1. Archives (Daily Readiness Records)

- [x] 1.1 Create `src/app/admin/archives/loading.tsx` with skeleton matching: title + subtitle, chart block (h-64), 4-field filter bar, 2-column record card grid (4 cards), 5-column summary stats row, export bar

## 2. Fleet Dashboard

- [x] 2.1 Replace `src/app/admin/loading.tsx` with a skeleton matching: title + subtitle, fleet matrix card (large), recent issues section, comments section, exceptions section

## 3. Equipment Catalog

- [x] 3.1 Create `src/app/admin/equipment/loading.tsx` with skeleton matching: title + subtitle, search filter bar, add-item form, pagination bar, 5 catalog item rows

## 4. Units Detail (Unit Builder)

- [x] 4.1 Create `src/app/admin/units/[id]/loading.tsx` with skeleton matching: header block (title + action buttons), add-compartment form, import-compartment form, kit assignment forms, 2 compartment section cards

## 5. Kits Detail (Kit Builder)

- [x] 5.1 Create `src/app/admin/kits/[id]/loading.tsx` with skeleton matching: header block, 2-column layout (update form left, assigned units sidebar right), equipment section

## 6. System Log

- [x] 6.1 Create `src/app/admin/system-log/loading.tsx` with skeleton matching: title + subtitle, database usage card, 6-field filter bar, pagination bar, 4 expandable log row cards

## 7. Units and Kits Lists

- [x] 7.1 Create `src/app/admin/units/loading.tsx` with skeleton matching: title + subtitle, 3 unit card placeholders, create-unit form
- [x] 7.2 Create `src/app/admin/kits/loading.tsx` with skeleton matching: title + subtitle, 2 create-form placeholders, 3 kit card placeholders

## 8. Analytics and Archive Detail

- [x] 8.1 Create `src/app/admin/analytics/loading.tsx` with skeleton matching: title + subtitle, 4-field filter form, 4 provider stats card placeholders
- [x] 8.2 Create `src/app/admin/archives/[id]/loading.tsx` with skeleton matching: breadcrumb heading, 3-column metadata grid, content cards

## 9. Verify

- [x] 9.1 Run `npm run typecheck` and `npm run lint` to verify no errors
- [x] 9.2 Manual smoke test: navigate to each page, verify loading skeleton appears and structurally matches the final page
