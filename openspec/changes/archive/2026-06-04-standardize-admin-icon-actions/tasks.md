## 1. Shared Icons

- [x] 1.1 Add `IconFilter` component to `src/components/icons.tsx`.

## 2. Kits Pages

- [x] 2.1 Replace "Edit Kit" text link with `IconEdit` on `kits/page.tsx`.
- [x] 2.2 Replace "Delete" text button with `IconTrash` on `kits/page.tsx`.
- [x] 2.3 Replace per-row "Delete" text buttons for groups with `IconTrash` on `kits/[id]/page.tsx`.
- [x] 2.4 Replace per-row "Delete" text buttons for items with `IconTrash` on `kits/[id]/page.tsx`.

## 3. Unit Builder Page

- [x] 3.1 Replace per-row "Save" text buttons for groups with `IconSave` on `units/[id]/page.tsx`.
- [x] 3.2 Replace per-row "Delete" text buttons for groups with `IconTrash` on `units/[id]/page.tsx`.
- [x] 3.3 Replace per-item "Delete Item" text buttons with `IconTrash` on `units/[id]/page.tsx`.
- [x] 3.4 Replace "Remove" text button for compartments with `IconTrash` on `units/[id]/page.tsx`.
- [x] 3.5 Replace "Remove From Unit" text button for assigned kits with `IconTrash` on `units/[id]/page.tsx`.
- [x] 3.6 Replace "Edit Kit" text link for assigned kits with `IconEdit` on `units/[id]/page.tsx`.

## 4. System Log Page

- [x] 4.1 Add `IconFilter` to the Filter button on `system-log/page.tsx`.
- [x] 4.2 Add `IconCancel` to the Reset button on `system-log/page.tsx`.

## 5. Equipment Controls

- [x] 5.1 Add an up-arrow icon to the "Back to top" button on `equipment/equipment-catalog-controls.tsx`.

## 6. Verification

- [x] 6.1 Run TypeScript typecheck.
