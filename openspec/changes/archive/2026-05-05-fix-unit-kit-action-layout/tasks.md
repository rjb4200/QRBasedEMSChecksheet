## 1. Layout Update

- [x] 1.1 Update the parent container for Assign Kit and Clone forms so the forms do not collide at intermediate widths
- [x] 1.2 Replace rigid fixed column templates in the Assign Kit form with a responsive layout that keeps controls inside the form
- [x] 1.3 Replace rigid fixed column templates in the Clone form with a responsive layout that keeps controls inside the form
- [x] 1.4 Preserve existing field names, placeholders, button labels, and form actions

## 2. Responsive Verification

- [x] 2.1 Verify the forms are readable and separated on desktop width
- [x] 2.2 Verify the forms stack or wrap cleanly on tablet/mobile widths
- [x] 2.3 Verify Assign Kit still submits the existing assign-kit action
- [x] 2.4 Verify Clone still submits the existing clone-kit-to-compartment action

## 3. Validation

- [x] 3.1 Run `npm run lint`
- [x] 3.2 Run `npm run typecheck`
