## ADDED Requirements

### Requirement: Fleet operations share a visual panel
The Fleet Panel SHALL display the page title, unit card grid, and daily checksheet print bar within a shared rounded background panel.

#### Scenario: Fleet operations panel renders
- **WHEN** the Fleet Panel loads
- **THEN** the title, unit cards, and print bar SHALL appear inside a single shared container with white background and rounded corners
