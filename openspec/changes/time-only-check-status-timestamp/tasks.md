## 1. Print Page

- [x] 1.1 Add `formatTimeOnly` helper to `src/app/admin/archives/print/page.tsx` and use it under Check Status for checked units.

## 2. PDF Generator

- [x] 2.1 Update `formatTimestamp` usage under Check Status in `src/lib/pdf/daily-checksheets.ts` to show time only.

## 3. Verification

- [x] 3.1 Run `npm run typecheck` and `npm run lint`.
