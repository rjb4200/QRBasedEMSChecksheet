## Context

The `color-unit-action-buttons` change made both Set OOS and QR buttons red on in-service units. The QR icon in red looks worse than plain slate. Only the Set OOS button should carry the red accent.

## Decisions

Revert QR button to always-slate styling. The Set OOS text button provides enough color anchor on its own.
