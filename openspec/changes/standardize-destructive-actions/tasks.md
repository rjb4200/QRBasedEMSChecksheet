## 1. Shared Component

- [x] 1.1 Create `src/components/delete-confirm-button.tsx` with two-stage confirm/cancel logic.
- [x] 1.2 Accept `formAction`, `hiddenInputs`, and optional `disabled` props.
- [x] 2.1 Replace single-click delete on `kits/page.tsx` with `DeleteConfirmButton`.
- [x] 2.2 Replace per-row `deleteKitGroup` buttons on `kits/[id]/page.tsx` with `DeleteConfirmButton`.
- [x] 2.3 Replace per-row `deleteKitItem` buttons on `kits/[id]/page.tsx` with `DeleteConfirmButton`.
- [x] 3.1 Replace `deleteUnitCompartment` with `DeleteConfirmButton`.
- [x] 3.2 Replace `deleteCompartmentGroup` with `DeleteConfirmButton`.
- [x] 3.3 Replace `deleteUnitItem` with `DeleteConfirmButton`.
- [x] 3.4 Replace `removeKitFromUnit` with `DeleteConfirmButton`.
- [x] 4.1 Add destructive mode toggle to the equipment catalog page.
- [x] 4.2 Hide delete icons when toggle is off, matching units/users pattern.
- [x] 5.1 Run TypeScript typecheck.
