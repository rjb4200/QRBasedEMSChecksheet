## 1. UI Cleanup

- [x] 1.1 Remove the per-unit action button row (View/No archive + Print) from each unit record card in `src/app/admin/archives/page.tsx`
- [x] 1.2 Verify the "Print Daily Record" button at the top of the page still functions correctly
